components:
  - name: Card
    description: Um componente de cartao simples.
    code: |
      import { Card, CardHeader, CardTitle, CardContent, CardFooter } from "@/components/ui/card";

      export default function CardExample() {
        return (
          <Card>
            <CardHeader>
              <CardTitle>Titulo do Card</CardTitle>
            </CardHeader>
            <CardContent>
              <p>Este e um exemplo de um componente de card usando shadcn UI.</p>
            </CardContent>
            <CardFooter>
              <p>Conteudo do rodape aqui.</p>
            </CardFooter>
          </Card>
        );
      }
    componentPath: "@/components/examples/CardExample"
    tags:
      - card
      - ui
      - shadcn
  - name: Button
    description: Um botao estilizado usando shadcn UI.
    code: |
      import { Button } from "@/components/ui/button";

      export default function ButtonExample() {
        return (
          <Button variant="primary">Clique Aqui</Button>
        );
      }
    componentPath: "@/components/examples/ButtonExample"
    tags:
      - button
      - ui
      - shadcn